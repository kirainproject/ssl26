import GameBreakOverlay from '@/components/GameBreakOverlay';

export const metadata = {
  title: 'SSL 2026 - Game Break',
  description: 'Game break/standby overlay for OBS',
};

export default function GameBreakPage() {
  return <GameBreakOverlay />;
}
