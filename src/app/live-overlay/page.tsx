import LiveOverlay from '@/components/LiveOverlay';

export const metadata = {
  title: 'SSL 2026 - Live Overlay',
  description: 'Live streaming overlay for OBS',
};

export default function LiveOverlayPage() {
  return <LiveOverlay />;
}
