import WinnerOverlay from '@/components/WinnerOverlay';

export const metadata = {
  title: 'SSL 2026 - Winner Overlay',
  description: 'Winner announcement overlay for OBS',
};

export default function WinnerOverlayPage() {
  return <WinnerOverlay />;
}
